import React from 'react';

class Prompt extends React.Component {
  // Create the prompt
  createPrompt(playerName) {
    
  }


  render() {
    return <div>{this.createPrompt(3, 3)}</div>;
  }
}

export default Prompt;
